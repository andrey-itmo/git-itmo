# git-itmo
My laboratory work for university
# 🧑‍💻 Лабораторная работа №3: Освоение системы контроля версий Git

**Автор:** Андрей Рыбалка
**Группа:** J3117
**Дата выполнения:** 7.11.2025

Данный репозиторий содержит результаты выполнения лабораторной работы по освоению основных команд и принципов системы контроля версий **Git**. В процессе работы были выполнены операции с ветками, слияния, перебазирование, откат коммитов и разрешение конфликтов.

---

## 🚀 Выполненные задания

В рамках работы были последовательно выполнены 8 основных заданий, охватывающих ключевые аспекты работы с Git:

### 1. Начальная настройка истории
- Создание первых коммитов в ветке `main` (`1)` и `2)`).
- Ветвление: создание ветки `backend_cuda` и добавление в нее коммита (`3)`).
- Анализ истории: сохранение разницы между коммитами в `diff.txt` (`4)`).

### 2. Манипуляции с историей (Reset)
- Откат ветки `main` к более раннему состоянию (`5)`).
- Добавление нового, замещающего коммита в ветку `main`.

### 3. Линеаризация истории (Rebase)
- Перебазирование ветки `backend_cuda` на обновленную ветку `main` (`6)`).
- **Цель:** Очистка истории и приведение ее к линейному виду.

### 4. Слияние и разрешение конфликтов (Merge)
- Создание двух независимых веток (`feature1` и `feature2`) и внесение в них изменений.
- Слияние `feature1` и `feature2` в `main` (`7)`).
- **Практика:** Разрешение конфликта, возникшего при слиянии, и создание merge-коммита.

### 5. Подготовка к релизу
- Создание стабильной ветки `release` на основе окончательного состояния `main` (`8)`).

---

## 🌳 Итоговая структура истории

Полная история коммитов со всеми ветками и слияниями сохранена в файле **`history.txt`**, который был сгенерирован командой:
```bash
git log --oneline --graph --all > history.txt
